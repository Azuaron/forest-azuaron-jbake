<!DOCTYPE html SYSTEM "http://www.thymeleaf.org/dtd/xhtml1-strict-thymeleaf-4.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xmlns:th="http://www.thymeleaf.org">
	<body>
	<div id="overallm" th:fragment="menu">
		<div id="menu">
			<a th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:href="${rootpath} + 'index.html'">
				<img id='logo' th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:src="${rootpath} + 'images/logo.png'" alt='Forest Azuaron' />
			</a>
			<ul class="nav navbar-nav">
				<li><a th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:href="${rootpath}+'index.html'">Home</a></li>
				<li><a th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:href="${rootpath}+'about.html'">About</a></li>
				<li><a th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" th:href="${rootpath}+${config.feed_file}">Subscribe</a></li>
			</ul>
			<img id='menurightborder' src='<?php bloginfo('template_directory'); ?>/images/menuright.png' alt='Menu right border' />
		</div><!-- end of menu -->
		<div id="mgear">
			<img id='topgear' th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" thsrc="${rootpath} + 'images/smallwheel.png'" alt='Small gear wheel' />
			<img id='bottomgear' th:with="rootpath=(${content.rootpath != null} ? ${content.rootpath} : '')" thsrc="${rootpath} + 'images/smallwheel.png'" alt='Small gear wheel' />
		</div>
	</div><!-- end of overallm -->
  	</body>
</html>
